---
interface Props {
  name: string;
  size?: string | number;
  class?: string;
  color?: string;
  title?: string;
}

const { name } = Astro.props as Props;

---

<img src={`/images/icon/${name}.svg`} alt={name} />